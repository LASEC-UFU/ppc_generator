# Perfil: perfil_minimo

Perfil Mínimo de Testes

Status: rascunho — gerado por `python -m ppcgen perfil-criar`. Preencha `perfil.yaml`, `matriz_curricular.xlsx`, `referenciais/` e `textos/` antes de validar.
